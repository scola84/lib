title: Elevate (password)

participant person
participant client
participant server

client->person:show password field\nshow next button
person->client:enter password\nclick next button
client->server:send password (body)\nPOST /api/auth/elevate/password
server->server:verify password\ngenerate token\nsave token to store
server->client:send token (cookie)
client->client:invoke request callback
